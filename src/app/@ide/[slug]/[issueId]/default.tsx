"use client";

import { Suspense } from "react";
import { IdeCommon } from "../../ideDefault";

export default function IDESlugDefault() {
  return (
    <Suspense>
      <IdeCommon />
    </Suspense>
  );
}
